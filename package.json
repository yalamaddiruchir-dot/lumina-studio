{
  "name": "lumina-studio",
  "version": "1.1.0",
  "private": true,
  "description": "Lumina Studios — full-stack multimedia company management app",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install && npm --prefix client install",
    "start": "node server/index.js",
    "server": "node server/index.js",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "dev": "npm run server & npm run client",
    "seed": "node server/seed.js",
    "create:admin": "node scripts/create-admin.js",
    "backup": "node scripts/backup.js",
    "reset:db": "node -e \"require('fs').rmSync('data/lumina.db',{force:true});require('fs').rmSync('data/lumina.db-wal',{force:true});require('fs').rmSync('data/lumina.db-shm',{force:true});console.log('DB removed — it will re-seed on next server start')\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.3.0"
  }
}
